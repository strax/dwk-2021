apiVersion: v1
kind: Service
metadata:
  name: dwk-server-svc
spec:
  type: NodePort
  selector:
    app: dwk-server
  ports:
    - name: http
      nodePort: 30080
      protocol: TCP
      port: 1234
      targetPort: 80