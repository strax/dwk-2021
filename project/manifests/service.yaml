apiVersion: v1
kind: Service
metadata:
  name: project-frontend-svc
spec:
  type: NodePort
  selector:
    app: project
  ports:
    - name: http
      nodePort: 30080
      port: 80
      protocol: TCP
      targetPort: 80